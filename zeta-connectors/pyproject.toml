[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "zeta-connectors"
version = "0.1.0"
description = "The bundled Zeta event connectors."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
authors = [
  { name = "Rémi Louf" }
]
dependencies = [
  "httpx",
  "zeta-os",
]

[project.optional-dependencies]
slack = ["websockets>=12"]

[project.entry-points."zeta.event_connectors"]
filesystem = "zeta_connectors.filesystem:main"
slack = "zeta_connectors.slack:main"
telegram = "zeta_connectors.telegram:main"
pushover = "zeta_connectors.pushover:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
