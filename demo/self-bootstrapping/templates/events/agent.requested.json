{
  "type": "object",
  "required": ["slug", "name", "watch_event", "limit_usd"],
  "properties": {
    "slug": { "type": "string" },
    "name": { "type": "string" },
    "watch_event": { "type": "string" },
    "limit_usd": { "type": "number" }
  }
}
