# Zeta Daytona executor

This package provides the `daytona` executor driver for Zeta.

The profile can set `daytona.snapshot`. The snapshot must have Python 3.11 or
newer. The driver copies the portable runtime into each verified sandbox.
