# Zeta Modal executor

This package provides the `modal` executor driver for Zeta.

The selected profile must set `modal.app`. A reusable profile needs a deployed
Modal app because Modal resolves a sandbox name inside that app.
